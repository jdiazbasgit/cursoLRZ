export interface MusicoInterface{
// Se coloca void (vacio) porque es vacio, no se conoce, entonces devolverá algo vacio, cuando queramos que devuelva algo, se le colocaría string y tendria un return
     tocar():void;
}